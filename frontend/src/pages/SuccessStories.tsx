import { CheckCircle2, Mail, MapPin, MessageCircle, Quote, Send, ZoomIn } from "lucide-react";
import { useState } from "react";
import type { FormEvent } from "react";
import Placeholder from "@/components/site/Placeholder";
import SectionHeading from "@/components/site/SectionHeading";
import SiteLayout from "@/components/site/SiteLayout";
import { testimonials, whatsappLink } from "@/lib/site";

const certificates = [["Aarav Sharma", "N1", "2024"], ["Priya Singh", "N3", "2023"], ["Kunal Verma", "N2", "2024"], ["Meera Gupta", "N4", "2022"], ["Rohan Malik", "N1", "2025"], ["Ananya Jain", "N3", "2024"]];

interface FormState { name: string; phone: string; email: string; level: string; batch: string; message: string; }
const initialForm: FormState = { name: "", phone: "", email: "", level: "", batch: "", message: "" };

function buildWhatsAppUrl(form: FormState) {
  const message = [
    "New Japanese class inquiry — RSP in Japan",
    `Name: ${form.name.trim()}`,
    `Phone: ${form.phone.trim()}`,
    `Email: ${form.email.trim()}`,
    `Current Japanese level: ${form.level}`,
    `Preferred batch: ${form.batch}`,
    `Message: ${form.message.trim()}`,
  ].join("\n");
  return `${whatsappLink}?text=${encodeURIComponent(message)}`;
}

export default function SuccessStories() {
  const [selectedCertificate, setSelectedCertificate] = useState<number | null>(null);
  const [form, setForm] = useState<FormState>(initialForm);
  const [submitted, setSubmitted] = useState(false);
  const [error, setError] = useState("");

  const updateField = (field: keyof FormState, value: string) => setForm((current) => ({ ...current, [field]: value }));
  const handleSubmit = (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); if (!form.name || !form.phone || !form.email || !form.level || !form.batch || !form.message) { setError("Please complete every field so Rohit can reply with the right next step."); return; } setError(""); window.open(buildWhatsAppUrl(form), "_blank", "noopener,noreferrer"); setSubmitted(true); };

  return <SiteLayout>
    <section data-testid="stories-hero" className="bg-[#241b18] px-5 pb-20 pt-40 text-[#fffaf2] md:px-10 md:pb-28 md:pt-48"><div className="mx-auto grid max-w-[1360px] gap-10 md:grid-cols-[1fr_.55fr] md:items-end"><div><p data-testid="stories-hero-eyebrow" className="text-[10px] font-bold uppercase tracking-[0.28em] text-[#e7c36d]">Success stories · Contact</p><h1 data-testid="stories-hero-heading" className="mt-5 max-w-4xl font-display text-6xl leading-[.9] tracking-[-.06em] md:text-8xl">The certificate is a moment.<br /><span className="text-[#e7c36d]">The life is the story.</span></h1></div><p data-testid="stories-hero-copy" className="max-w-sm text-sm leading-7 text-[#f7f1e8]/65">Every student arrives with a different reason. A shared thread runs through them all: they kept going.</p></div></section>
    <section data-testid="stories-certificates-section" className="wash-pattern px-5 py-24 md:px-10 md:py-32"><div className="mx-auto max-w-[1360px]"><div className="flex flex-col justify-between gap-8 md:flex-row md:items-end"><SectionHeading eyebrow="Certificates" title="Proof of the days no one else saw." description="A growing gallery of JLPT milestones. More certificate images will be added here as new students reach their next level." /><p data-testid="stories-certificate-note" className="max-w-xs text-xs leading-6 text-[#756862]">Click any card to enlarge the placeholder and swap in the real certificate image later.</p></div><div className="mt-14 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{certificates.map(([name, level, year], index) => <button type="button" key={name} data-testid={`certificate-card-${index}`} onClick={() => setSelectedCertificate(index)} className="group text-left transition-transform duration-300 hover:-translate-y-2"><div className="relative"><Placeholder label={`[PLACEHOLDER: ${name} ${level} certificate]`} aspect="landscape" /><span data-testid={`certificate-zoom-icon-${index}`} className="absolute right-4 top-4 rounded-full bg-[#fffaf2]/90 p-2 text-[#a80000] opacity-0 transition-opacity duration-200 group-hover:opacity-100"><ZoomIn size={15} /></span></div><div className="mt-4 flex items-end justify-between border-t border-[#a80000]/20 pt-3"><div><p data-testid={`certificate-student-${index}`} className="font-display text-2xl">{name}</p><p data-testid={`certificate-level-${index}`} className="mt-1 text-[10px] font-bold uppercase tracking-[0.15em] text-[#a80000]">JLPT {level}</p></div><span data-testid={`certificate-year-${index}`} className="text-xs text-[#756862]">{year}</span></div></button>)}</div></div></section>
    <section data-testid="stories-testimonials-section" className="bg-[#a80000] px-5 py-24 text-[#fffaf2] md:px-10 md:py-32"><div className="mx-auto max-w-[1360px]"><SectionHeading light eyebrow="Student voices" title="What happens when the language becomes yours." /><div className="mt-14 grid gap-4 md:grid-cols-3">{testimonials.map((item, index) => <article key={item.name} data-testid={`testimonial-card-${index}`} className="border border-[#f7f1e8]/20 p-7"><Quote data-testid={`testimonial-quote-icon-${index}`} size={25} className="text-[#e7c36d]" /><p data-testid={`testimonial-quote-${index}`} className="mt-12 min-h-28 font-display text-2xl leading-tight">“{item.quote}”</p><div className="mt-8 border-t border-[#f7f1e8]/20 pt-4"><p data-testid={`testimonial-name-${index}`} className="text-xs font-bold">{item.name}</p><p data-testid={`testimonial-meta-${index}`} className="mt-1 text-[10px] uppercase tracking-[0.15em] text-[#f7f1e8]/55">{item.level} · {item.place}</p></div></article>)}</div></div></section>
    <section id="contact" data-testid="contact-section" className="bg-[#f7f1e8] px-5 py-24 md:px-10 md:py-32"><div className="mx-auto grid max-w-[1360px] gap-14 md:grid-cols-[.75fr_1.25fr] md:gap-24"><div><SectionHeading eyebrow="Start a conversation" title="Tell us where you want to go." description="Share a little about your current level and the life you are imagining. We will help you see the next step clearly." /><div className="mt-10 grid gap-4 text-sm text-[#756862]"><a href={whatsappLink} target="_blank" rel="noreferrer" data-testid="contact-whatsapp-link" className="flex items-center gap-3 hover:text-[#a80000]"><MessageCircle size={18} className="text-[#a80000]" /> WhatsApp Rohit</a><a href="mailto:hello@rspinjapan.com" data-testid="contact-email-link" className="flex items-center gap-3 hover:text-[#a80000]"><Mail size={18} className="text-[#a80000]" /> hello@rspinjapan.com</a><p data-testid="contact-location-lines" className="flex items-start gap-3"><MapPin size={18} className="mt-0.5 text-[#a80000]" /> Kyoto, Japan · Meerut, India</p></div><div data-testid="contact-map-placeholder" className="mt-10 flex aspect-[16/8] items-center justify-center border border-dashed border-[#a80000]/25 bg-[#eadfd2] px-5 text-center"><span className="text-[10px] font-bold uppercase tracking-[0.18em] text-[#8d302c]">[PLACEHOLDER: Kyoto + Meerut map embed]</span></div></div><div className="bg-[#fffaf2] p-6 shadow-[0_18px_60px_rgba(36,27,24,.08)] md:p-10">{submitted ? <div data-testid="contact-success-state" className="flex min-h-[500px] flex-col items-start justify-center"><CheckCircle2 data-testid="contact-success-icon" size={35} className="text-[#a80000]" /><p data-testid="contact-success-eyebrow" className="mt-8 text-[10px] font-bold uppercase tracking-[0.22em] text-[#a80000]">WhatsApp draft ready</p><h2 data-testid="contact-success-heading" className="mt-4 font-display text-5xl leading-none">Your next step<br />is now in motion.</h2><p data-testid="contact-success-copy" className="mt-5 max-w-md text-sm leading-7 text-[#756862]">Thank you, {form.name}. WhatsApp has opened with your level, preferred batch, and message ready to send to Rohit.</p><button type="button" data-testid="contact-open-whatsapp-button" onClick={() => window.open(buildWhatsAppUrl(form), "_blank", "noopener,noreferrer")} className="mt-8 inline-flex items-center gap-3 bg-[#a80000] px-5 py-3 text-[10px] font-bold uppercase tracking-[0.16em] text-[#fffaf2]">Open WhatsApp again <MessageCircle size={14} /></button><button type="button" data-testid="contact-reset-button" onClick={() => { setSubmitted(false); setForm(initialForm); }} className="mt-5 border-b border-[#a80000] pb-2 text-[10px] font-bold uppercase tracking-[0.17em] text-[#a80000]">Send another inquiry</button></div> : <form data-testid="contact-form" onSubmit={handleSubmit} noValidate><p data-testid="contact-form-eyebrow" className="text-[10px] font-bold uppercase tracking-[0.22em] text-[#a80000]">Inquiry form</p><h2 data-testid="contact-form-heading" className="mt-4 font-display text-4xl leading-tight">Let’s map your route.</h2><div className="mt-8 grid gap-6 sm:grid-cols-2"><Field label="Your name" id="contact-name" value={form.name} onChange={(value) => updateField("name", value)} /><Field label="Phone number" id="contact-phone" type="tel" value={form.phone} onChange={(value) => updateField("phone", value)} /><Field label="Email address" id="contact-email" type="email" value={form.email} onChange={(value) => updateField("email", value)} /><SelectField label="Current Japanese level" id="contact-level" value={form.level} options={["Just starting", "N5", "N4", "N3", "N2", "N1"]} onChange={(value) => updateField("level", value)} /><SelectField label="Preferred batch" id="contact-batch" value={form.batch} options={["Online", "Offline in Meerut", "Weekend", "One-on-one intensive"]} onChange={(value) => updateField("batch", value)} /><div className="sm:col-span-2"><label htmlFor="contact-message" data-testid="contact-message-label" className="text-[10px] font-bold uppercase tracking-[0.15em] text-[#756862]">Tell us your goal</label><textarea id="contact-message" data-testid="contact-message-input" value={form.message} onChange={(event) => updateField("message", event.target.value)} rows={4} placeholder="Where would you like this journey to take you?" className="mt-2 w-full resize-none border-0 border-b border-[#241b18]/20 bg-transparent px-0 py-3 text-sm outline-none transition-colors duration-200 placeholder:text-[#756862]/60 focus:border-[#a80000]" /></div></div>{error && <p role="alert" data-testid="contact-error-message" className="mt-5 text-xs font-bold leading-5 text-[#a80000]">{error}</p>}<button type="submit" data-testid="contact-submit-button" className="mt-8 inline-flex items-center gap-3 bg-[#a80000] px-6 py-4 text-[10px] font-bold uppercase tracking-[0.16em] text-[#fffaf2] transition-transform duration-200 hover:-translate-y-1">Send via WhatsApp <Send size={14} /></button><p data-testid="contact-form-footnote" className="mt-5 text-[10px] leading-5 text-[#756862]">Your inquiry opens in WhatsApp for Rohit. Review the draft and tap send.</p></form>}</div></div></section>
    {selectedCertificate !== null && <div role="dialog" aria-modal="true" aria-label="Certificate preview" data-testid="certificate-lightbox" className="fixed inset-0 z-[60] flex items-center justify-center bg-[#241b18]/90 p-5" onClick={() => setSelectedCertificate(null)}><div className="w-full max-w-xl bg-[#f7f1e8] p-5" onClick={(event) => event.stopPropagation()}><Placeholder label={`[PLACEHOLDER: ${certificates[selectedCertificate][0]} ${certificates[selectedCertificate][1]} certificate enlarged]`} aspect="landscape" /><div className="flex items-center justify-between pt-5"><div><p data-testid="lightbox-student" className="font-display text-2xl">{certificates[selectedCertificate][0]}</p><p data-testid="lightbox-meta" className="mt-1 text-[10px] font-bold uppercase tracking-[0.15em] text-[#a80000]">JLPT {certificates[selectedCertificate][1]} · {certificates[selectedCertificate][2]}</p></div><button type="button" data-testid="certificate-lightbox-close" onClick={() => setSelectedCertificate(null)} className="text-[10px] font-bold uppercase tracking-[0.15em] text-[#a80000]">Close</button></div></div></div>}
  </SiteLayout>;
}

function Field({ label, id, value, onChange, type = "text" }: { label: string; id: string; value: string; onChange: (value: string) => void; type?: string }) { return <div><label htmlFor={id} data-testid={`${id}-label`} className="text-[10px] font-bold uppercase tracking-[0.15em] text-[#756862]">{label}</label><input id={id} type={type} data-testid={`${id}-input`} value={value} onChange={(event) => onChange(event.target.value)} className="mt-2 w-full border-0 border-b border-[#241b18]/20 bg-transparent px-0 py-3 text-sm outline-none transition-colors duration-200 placeholder:text-[#756862]/60 focus:border-[#a80000]" /></div>; }
function SelectField({ label, id, value, options, onChange }: { label: string; id: string; value: string; options: string[]; onChange: (value: string) => void }) { return <div><label htmlFor={id} data-testid={`${id}-label`} className="text-[10px] font-bold uppercase tracking-[0.15em] text-[#756862]">{label}</label><select id={id} data-testid={`${id}-input`} value={value} onChange={(event) => onChange(event.target.value)} className="mt-2 w-full border-0 border-b border-[#241b18]/20 bg-transparent px-0 py-3 text-sm outline-none transition-colors duration-200 focus:border-[#a80000]"><option value="">Select one</option>{options.map((option) => <option key={option} value={option}>{option}</option>)}</select></div>; }